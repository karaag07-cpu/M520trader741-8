"""Pytest configuration.

Placing this file at the repository root puts the project root on ``sys.path``
during test collection, so tests can import the top-level packages
(``signals``, ``execution``, ``backtest``, ``risk``, ``utils`` ...) regardless
of the directory pytest is invoked from.
"""
